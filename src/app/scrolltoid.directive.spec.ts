import { ScrolltoidDirective } from './scrolltoid.directive';

describe('ScrolltoidDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolltoidDirective();
    expect(directive).toBeTruthy();
  });
});
