import { ImgloaderrorDirective } from './imgloaderror.directive';

describe('ImgloaderrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgloaderrorDirective();
    expect(directive).toBeTruthy();
  });
});
